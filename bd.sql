CREATE DATABASE CV;
CREATE TABLE datos(
    email varchar(30),
    nombre varchar(30),
    apellido varchar(30),
    telefono varchar(30),
    ciudad varchar(30),
    codigo varchar(30)
    );

    use cv;

    CREATE TABLE datos(
    email varchar(30),
    nombre varchar(30),
    apellido varchar(30),
    telefono varchar(30),
    ciudad varchar(30),
    codigo varchar(30)
    );
